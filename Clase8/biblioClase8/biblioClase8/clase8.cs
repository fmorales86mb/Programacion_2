/*
 * Clase 8 
 * 
 * ENCAPSULAMIENTO
 * 
 * Es el ocultamiento del estado. Mostramos parte, lo que se necesite.
 * Sòlo se pueden cambiar mediante las operaciones definidas para eso.
 * Objetos aislados del exterior
 * Puede ocultar procesos internos.
 * 
 * 3 niveles: 
 * publico
 * protegido, solo accesibles por su clase y subclases
 * privado.
 * 
 * propiedades: mecanismo para leer o escribir o calcular.
 * se escriben con mayusculas.
 * devolver el valor Get (return), para asignar Set (value)
 * 
 * De ahora en más se crean los atributos de la clase privados o protegidos. Se crean las propiedades,
 * con o sin los descriptores de acceso (get, set), para utilizar los atributos.
 * 
 * INDEXADORES
 * 
 * ENUMERADOR
 * 
 * lo usaba en sigeci. Conjuto de constantes con nombre. Se usa en atributos.
 * Se asocia a un entero. Se puede definir en la clase o en fuera de la clase, dentro del namesapece
 * 
 */